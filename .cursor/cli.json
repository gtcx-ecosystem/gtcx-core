{
  "permissions": {
    "allow": [
      "Shell(git)",
      "Shell(git **)",
      "Shell(git push)",
      "Shell(git push **)",
      "Shell(git pull **)",
      "Shell(git fetch **)",
      "Shell(gh)",
      "Shell(gh **)",
      "Shell(pnpm **)",
      "Shell(node **)",
      "Shell(npx **)",
      "Shell(baseline)",
      "Shell(baseline **)",
      "Shell(python3 **)",
      "Shell(cargo **)"
    ],
    "deny": [
      "Shell(git push --force **)",
      "Shell(git push ** -f)",
      "Shell(git push ** --force)",
      "Shell(git reset --hard **)",
      "Shell(git clean -fd **)"
    ]
  }
}

# Alpha Testing

_Not applicable to `gtcx-core`. This repo publishes versioned npm packages and Rust crates — it does not run alpha cohort programs or user acceptance phases._

For packages under active development (pre-1.0), use pre-release semver identifiers (`0.x.y` or `1.0.0-alpha.1`). See `_sop/5-release/5-versioning/versioning-policy.md` for the versioning policy.

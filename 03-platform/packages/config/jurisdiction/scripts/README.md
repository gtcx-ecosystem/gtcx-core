# Jurisdiction config scripts

Placeholder for jurisdiction-pack validation and maintenance scripts (`validate-packs`, future codegen).

Primary entry today: `pnpm jurisdiction:validate-packs` (root `package.json` → `@gtcx/jurisdiction-config`).

# packages

GTCX TypeScript packages: **24** runtime `@gtcx/*` workspace packages plus **4** shared config packages under `packages/config/` (28 total workspace entries; see `pnpm-workspace.yaml`).

See `docs/specs/packages/` for per-package contracts and `docs/architecture/overview.md` for layering.

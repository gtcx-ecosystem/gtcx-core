# @gtcx/utils

Small utility helpers used across packages.

## Scope

- ID generation
- Sleep + retry helpers
- JSON parsing with optional Zod validation
- Object helpers (`pick`, `omit`, `deepClone`)
- Debounce/throttle

## References

- `packages/utils/src/index.ts`

# API Surface Baseline

This document points to the canonical API surface baselines and reports.

- Baseline: `quality/api-surface-baseline.json`
- Latest report (generated): `quality/api-surface-report.json`

Use `pnpm api:check` to validate changes against the baseline.

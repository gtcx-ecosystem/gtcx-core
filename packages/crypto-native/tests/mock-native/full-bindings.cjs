module.exports = {
  generateKeyPair: () => ({ privateKey: 'aabb', publicKey: 'ccdd' }),
  sign: (msg, pk) => 'eeff',
  verify: (sig, msg, pub) => true,
  sha256: (data) => 'sha256hash',
  sha512: (data) => 'sha512hash',
  blake3_hash: (data) => 'blake3hash',
  derive_child_key: (key, idx) => 'childkey',
  derive_purpose_key: (key, label) => 'purposekey',
  version: () => '1.0.0',
  groth16_generate_keys: (circuit) => ({ circuit, provingKey: 'pk', verifyingKey: 'vk' }),
  groth16_prove_gci_threshold: (score, threshold, pk, vk) => ({
    circuit: 'gci',
    proof: 'proof',
    verifyingKey: vk,
    publicInputsJson: '{}',
  }),
  groth16_verify_proof: (circuit, proof, vk, inputs) => true,
  bulletproofs_prove_amount_range: (amount, min, max, randomness) => ({
    min,
    max,
    commitment: 'commit',
    proofLow: 'low',
    proofHigh: 'high',
  }),
  bulletproofs_verify_amount_range: (min, max, commit, low, high) => true,
  schnorr_prove_identity_attribute: (attr, subject) => ({
    attributeHash: 'ah',
    subjectHash: subject,
    nonceCommitment: 'nc',
    response: 'resp',
  }),
  schnorr_verify_identity_attribute: (ah, sh, nc, resp) => true,
};
